query GetNote($id: Int!, $userId: String!) {
  note(id: $id, userId: $userId) {
    id
    entityId
    entityType
    userId
    noteType
    content
    createdBy
    tags
    createdAt
    updatedAt
  }
}
