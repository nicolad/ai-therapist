mutation CreateNote($input: CreateNoteInput!) {
  createNote(input: $input) {
    id
    entityId
    entityType
    userId
    noteType
    slug
    content
    createdBy
    tags
    createdAt
    updatedAt
  }
}
